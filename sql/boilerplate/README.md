# Boilerplate Driver

Here, you will find the basic boilerplate needed to construct a new driver for `dbprove`.

When creating a driver, start by copying this directory and modifying the contents of the files as needed.